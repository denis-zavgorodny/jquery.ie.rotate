Plug-in for the rotation of DOM elements in IE versions 8 and below.
The plugin requires the jQuery library.
--------------------------------------------------------------------
Calling the plugin:
jQuery (selector). rotate (options);

options (obj):
deg (int) - angle from the horizontal element (clockwise)

